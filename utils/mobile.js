const fast2sms = require('fast-two-sms');

module.exports.fast2sms = async ({ message, otp }) => {
    try {
        const res = await fast2sms.sendMessage({

        })
    } catch (error) {

    }
}